import React from 'react';

import './Header.css';

function Header() {
    return(
        <header className="header">
            <div>
                <h1>Produtos CRUD</h1>
            </div>
        </header>
    );
}

export default Header;